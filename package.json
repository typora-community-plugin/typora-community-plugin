{
  "private": true,
  "name": "typora-community-plugin",
  "version": "",
  "description": "",
  "engines": {
    "node": ">=18",
    "pnpm": ">=7"
  },
  "scripts": {
    "build": "cd packages/loader && pnpm run build &&  cd ../core && pnpm run build &&  cd ../.. && node build.js"
  },
  "keywords": [],
  "author": "plylrnsdy",
  "license": "MIT",
  "devDependencies": {
    "archiver": "^5.3.1"
  }
}
