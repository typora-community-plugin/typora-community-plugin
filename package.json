{
  "private": true,
  "name": "typora-community-plugin",
  "version": "",
  "description": "",
  "engines": {
    "node": ">=18",
    "pnpm": ">=7"
  },
  "scripts": {
    "build": "cd packages/rollup-plugin && pnpm run build &&  cd ../loader && pnpm run build &&  cd ../core && pnpm run build &&  cd ../installer && pnpm run build &&  cd ../.. && node build.js"
  },
  "keywords": [],
  "author": "plylrnsdy",
  "license": "MIT",
  "devDependencies": {
    "archiver": "^5.3.1"
  }
}
